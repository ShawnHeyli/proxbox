aliases="
# Common aliases from proxbox
alias rm='rm --verbose'
alias cp='cp --verbose'
alias mv='mv --verbose'
alias chmod='chmod --changes'
alias chown='chown --changes'
alias mkdir='mkdir --parents --verbose'
"

echo $aliases >> ~/.bashrc